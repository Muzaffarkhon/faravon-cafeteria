/**
 * Реальные данные диалога подгружает `ThreadViewLive` (клиентский компонент
 * в `../layout.tsx`) через `/api/support/thread/<id>` — эта страница нужна
 * только чтобы у Next.js был маршрут `/admin/support/<id>`; никакой
 * серверной работы она не делает, поэтому клик по чату не запускает RSC-
 * навигацию и её Suspense-заглушку — правая панель просто меняет
 * содержимое на месте, без перезагрузки страницы.
 */
export default function SupportThreadPage() {
  return null;
}
