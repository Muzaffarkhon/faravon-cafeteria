Добавляет поддержку баннеров партнёров и заявки на рекламу, API для подтверждения купона у подрядчика и UI для админки.

Важно:
- Содержит миграцию: prisma/migrations/20260901221727_roles_and_banners/migration.sql (изменение enum Role + новые таблицы).
- Перед деплоем в production: сделать бэкап БД и выполнить `npx prisma migrate deploy` или применить SQL вручную.

Чеклист:
- [ ] Бэкап production БД
- [ ] Применить миграцию в staging
- [ ] Проверить функциональность (логины, RBAC, баннеры)
- [ ] Слить в main и задеплоить на Vercel

Тесты/проверки:
- Локально: `npm run typecheck` и `npm run build` (выполнены).
- PR создаёт preview-деплой на Vercel, проверить UI через ссылку preview.